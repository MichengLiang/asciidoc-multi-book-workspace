== 跨卷实践

下册通过属性保存上册入口路径。这样引用目标集中在 `book.adoc` 中维护，章节正文只表达语义关系。

[source,asciidoc]
----
:upper-book: ../05-upper-volume/book.adoc
xref + : + {upper-book}#upper-core-model[上册核心模型]
----

实际写作时去掉 `xref` 后面的空格和加号，写成一个完整的 cross reference 宏。
