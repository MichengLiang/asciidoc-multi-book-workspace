[#default-semantics]
== 默认语义

等号标题默认形成书稿结构单位。普通标题可以不写 role；不写 role 时，标题仍然可以被阅读、编号、引用和投影为 heading node。

重要标题可以补充稳定 ID。稳定 ID 是作者声明的引用地址，适合长期引用、跨章节引用和工具链回源。

没有 `rel` 的 xref 表示普通引用。普通引用适合参见、导航和弱提及；它不是缺少关系，而是默认 references。

显式 `rel` 用于可说明的强关系。作者写下 `rel` 时，也写下了关系谓词的解释责任。
